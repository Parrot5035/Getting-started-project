using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CubeRotation : MonoBehaviour {

    //example comment
    //hilight words and hit command ' to get linked to the API
    //var example float int string boolean vector3(cords) game objects can also be var
    public float rotateSpeed;

    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
	
        transform.Rotate

	}
}
